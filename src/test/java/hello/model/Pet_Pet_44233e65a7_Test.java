// Test generated by RoostGPT for test petstore-java using AI Type Open AI and AI Model gpt-4

package hello.model;

import jakarta.persistence.Entity;
import jakarta.persistence.Table;
import jakarta.persistence.Id;
import jakarta.persistence.GeneratedValue;
import jakarta.persistence.GenerationType;
import jakarta.persistence.Column;
import jakarta.validation.constraints.NotNull;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;

@SpringBootTest
public class Pet_Pet_44233e65a7_Test {

    @MockBean
    private Pet pet;

    @BeforeEach
    public void setUp() {
        Mockito.when(pet.getName()).thenReturn("Tommy");
        Mockito.when(pet.getAge()).thenReturn(5);
    }

    @Test
    public void testPetConstructor() {
        Assertions.assertEquals("Tommy", pet.getName());
        Assertions.assertEquals(5, pet.getAge());
    }

    @Test
    public void testPetConstructorFailure() {
        Mockito.when(pet.getName()).thenReturn(null);
        Assertions.assertNull(pet.getName());

        Mockito.when(pet.getAge()).thenReturn(null);
        Assertions.assertNull(pet.getAge());
    }
}
